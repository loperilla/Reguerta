<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Pre-Android 12 (fallback): usa DayNight M3 y un windowBackground que lee de un drawable con atributos de tema -->
    <style name="LaunchTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- Tema normal básico por si se usa en otros contextos -->
    <style name="NormalTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">?attr/colorSurface</item>
    </style>
</resources>